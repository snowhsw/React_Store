import styled from "styled-components";

const MainWrap = styled.main`
    max-width: 1280px;
    margin: auto;
    background-color: rgb(249, 250, 251);
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
`;

export {MainWrap}